<!DOCTYPE html>
<html lang="en">
<h1>Your Locations:</h1>
<% if @locations.any? %>
  <ul>
    <% @locations.each do |location| %>
      <li>
        <strong><%= location.name %></strong> 
        (<%= location.latitude %>, <%= location.longitude %>)
        <%= link_to 'Get Forecast', forecast_location_path(location) %>
        <%= link_to 'Delete', location_path(location), data: { turbo_method: :delete, turbo_confirm: 'Are you sure you want to delete this recipe? Deleting a recipe cannot be undone.' }, method: :delete %>

      </li>
    <% end %>
  </ul>

<% else %>
  <h1>Please add a location to see your forecast.</h1>
<% end %>

<%= link_to 'Add Location', new_location_path %>
</html>
