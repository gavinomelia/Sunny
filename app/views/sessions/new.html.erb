<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Login</title>
  </head>
  <body class="bg-gray-100 flex justify-center items-center min-h-screen m-0">

    <div class="w-full max-w-md bg-white p-8 rounded shadow-lg mx-auto mt-28">
      <!-- Text Login -->
      <h1 class="text-center text-2xl font-bold text-gray-700 mb-6">LOGIN</h1>
      <%= form_with(url: login_path, local: true, class: "space-y-6") do |form| %>
        <!-- Email -->
        <div>
          <%= form.label :email, "Email", class: "block text-gray-700 font-medium mb-1" %>
          <%= form.email_field :email, class: "w-full py-2 px-4 bg-gray-200 rounded focus:ring focus:outline-blue-500", placeholder: "Email" %>
        </div>
        <!-- Password -->
        <div>
          <%= form.label :password, "Password", class: "block text-gray-700 font-medium mb-1" %>
          <%= form.password_field :password, class: "w-full py-2 px-4 bg-gray-200 rounded focus:ring focus:outline-blue-500", placeholder: "Password" %>
        </div>
        <!-- Submit -->
        <div>
          <%= form.submit "Login", class: "w-full py-2 bg-blue-500 text-white font-bold rounded hover:bg-blue-600" %>
        </div>
      <% end %>

      <!-- Sign Up -->
      <p class="text-center text-gray-600 mt-4">
      Don't have an account? 
      <%= link_to "Sign Up", new_user_path, class: "text-blue-500 font-medium hover:underline" %>
      </p>
    </div>

  </body>
</html>

