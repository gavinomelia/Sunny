<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Sign Up</title>
  </head>
  <body class="bg-gray-100 flex justify-center items-center h-screen m-0">

    <div class="w-full max-w-md bg-white p-8 rounded shadow-lg mx-auto">
      <h1 class="text-2xl font-bold text-gray-600 text-center mb-6">Sign Up</h1>
      <%= form_with(model: @user, local: true, class: "space-y-6") do |form| %>
        <!-- Email -->
        <div>
          <%= form.label :email, "Email", class: "block text-gray-700 font-medium mb-1" %>
          <%= form.email_field :email, class: "w-full py-2 px-4 bg-gray-200 rounded focus:ring focus:outline-blue-500", placeholder: "Email" %>
        </div>
        <!-- Password -->
        <div>
          <%= form.label :password, "Password", class: "block text-gray-700 font-medium mb-1" %>
          <%= form.password_field :password, class: "w-full py-2 px-4 bg-gray-200 rounded focus:ring focus:outline-blue-500", placeholder: "Password" %>
        </div>
        <!-- Password Confirmation -->
        <div>
          <%= form.label :password_confirmation, "Confirm Password", class: "block text-gray-700 font-medium mb-1" %>
          <%= form.password_field :password_confirmation, class: "w-full py-2 px-4 bg-gray-200 rounded focus:ring focus:outline-blue-500", placeholder: "Confirm Password" %>
        </div>
        <!-- Submit -->
        <div>
          <%= form.submit "Sign Up", class: "w-full py-2 bg-blue-500 text-white font-bold rounded hover:bg-blue-600" %>
        </div>
      <% end %>

      <!-- Login Link -->
      <p class="text-center text-gray-600 mt-4">
      Already have an account? 
      <%= link_to "Login", new_session_path, class: "text-blue-500 font-medium hover:underline" %>
      </p>
    </div>

  </body>
</html>

